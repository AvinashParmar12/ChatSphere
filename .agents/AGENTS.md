# Agent Rules

Whenever you propose an implementation plan:
- minimize the number of files
- reuse existing hooks
- reuse existing Redux slices
- reuse existing RTK Query endpoints
- reuse existing socket listeners

Prefer extending existing architecture over creating new abstractions.
